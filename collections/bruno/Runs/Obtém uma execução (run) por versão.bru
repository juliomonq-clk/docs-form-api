meta {
  name: Obtém uma execução (run) por versão
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/versions/{{version_key}}/runs/{{run_key}}
  body: none
  auth: inherit
}

docs {
  Implementação `GET /api/v1/versions/{version_key}/runs/{run_key}`.
}
